{
  "version": 2,
  "outputDirectory": "frontend/dist",
  "buildCommand": "echo 'Frontend already pre-built in frontend/dist'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/$1"
    }
  ]
}
